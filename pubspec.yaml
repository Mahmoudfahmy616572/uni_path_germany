name: germany_travel
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  cached_network_image: ^3.4.1
  carousel_slider: ^5.1.2
  connectivity_plus: ^6.0.3
  cupertino_icons: ^1.0.6
  equatable: ^2.0.5
  file_picker: ^11.0.2
  firebase_auth: ^4.9.0
  firebase_core: ^2.15.0
  firebase_messaging: ^14.6.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_local_notifications: ^17.2.2
  flutter_screenutil: ^5.9.0
  flutter_staggered_animations: ^1.1.1
  font_awesome_flutter: ^11.0.0
  get_it: ^7.6.4
  go_router: ^12.1.0
  google_fonts: ^6.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.19.0
  logger: ^2.5.0
  path_provider: ^2.1.1
  supabase_flutter: ^2.0.0
  timezone: ^0.9.0
  workmanager: ^0.9.0

# ✅ هنا بالضبط - بعد dependencies وقبل dev_dependencies
dependency_overrides:
  http: ^1.2.0

dev_dependencies:
  build_runner: ^2.4.7
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1

flutter:

  uses-material-design: true
