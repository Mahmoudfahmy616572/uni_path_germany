name: ai_proxy_server
description: Backend proxy for Gemini AI calls
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_multipart: ^1.0.0
  google_generative_ai: ^0.4.6
  http_parser: ^4.0.0
