import 'package:flutter/material.dart';

class AppColors {
  // ── Light Theme ───────────────────────────────────────────
  static const Color primaryBlue = Color(0xFF1E293B);
  static const Color accentGold = Color(0xFFD4AF37);
  static const Color lightBackground = Color(0xFFF8FAFC);
  static const Color softBlue = Color(0xFF6366F1);
  static const Color textDark = Color(0xFF1E293B);
  static const Color textGrey = Color(0xFF64748B);
  static const Color primary = Color(0xFF5A67D8);
  static const Color background = Color(0xFFF7FAFC);
  static const Color inputBackground = Color(0xFFEDF2F7);

  // ── Dark Theme (من التصميم) ──────────────────────────────
  static const Color darkBackground = Color(0xFF0B0E14);
  static const Color darkSurface = Color(0xFF171C26);
  static const Color darkCardBg = Color(0xFF1E2533);
  static const Color darkBorder = Color(0xFF2A3040);
  static const Color primaryPurple = Color(0xFF6366F1);
  static const Color accentBlue = Color(0xFF38BDF8);
  static const Color textMain = Color(0xFFFFFFFF);
  static const Color textMuted = Color(0xFF94A3B8);

  // ── Match Card Gradient ───────────────────────────────────
  static const Color matchGradientStart = Color(0xFF4F46E5);
  static const Color matchGradientEnd = Color(0xFF3730A3);

  // ── Badges ────────────────────────────────────────────────
  static const Color badgeGreen = Color(0xFF10B981);
  static const Color badgeAmber = Color(0xFFF59E0B);
  static const Color badgeBlue = Color(0xFF38BDF8);

  // ── Progress / Sliders ────────────────────────────────────
  static const Color trackBg = Color(0xFF2E3748);

  // ── Chat ──────────────────────────────────────────────────
  static const Color chatBubbleUser = Color(0xFF312E81);
  static const Color chatLink = Color(0xFF818CF8);

  // ── Chip / Tab ────────────────────────────────────────────
  static const Color chipBorder = Color(0xFF2E3748);

  // ── Hero Overlay ──────────────────────────────────────────
  static const Color heroBtnBg = Color(0x66000000);

  // ── Avatar ────────────────────────────────────────────────
  static const Color avatarBg = Color(0xFF4B5563);

  // ── Profile Gradient ──────────────────────────────────────
  static const Color profileGradientStart = Color(0xFF312E81);
}
